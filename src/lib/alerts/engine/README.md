# ⚠️ Código morto — não editar

@deprecated — ver `lib/alerts/engine/README.md` (raiz, mesma investigação,
2026-09-06). Esta é a segunda cópia do mesmo pipeline morto
(`evaluatePolicies → enrichAlert → computeRiskScore → persistAlert →
auditAlert`) — nenhuma das duas (raiz ou aqui) tem consumidor real. O
pipeline vivo é `lib/domain/alerts/*`.

Não editar nem estender. Ver `CLAUDE.md` atualizado.
